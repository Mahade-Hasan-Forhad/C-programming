#include <stdio.h>
#include <math.h>
main()

{
    float radius, area;
    printf ("\n Enter the Radius of Circle : ");
    scanf ("%d", &radius);
    area = 3.1416*radius*radius;
    printf ("\n The Area of Circle : %f",area);
    return(0);

}
